<h1><%= @profile.name %></h1>

<% if @profile.avatar.attached? %>
  <%= image_tag @profile.avatar.variant(resize_to_limit: [300, 300]) %>
<% else %>
  <p>No image found.</p>
<% end %>

<p><%= @profile.introduction %></p>

<%= link_to 'プロフィールを編集', edit_profile_path %>